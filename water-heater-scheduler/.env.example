# Water Heater Scheduler local testing environment

# Home Assistant API configuration
HA_API_URL=http://your-homeassistant:8123/api
HA_API_TOKEN=your-long-lived-access-token

# Optional: Set to 1 to run single evaluation then exit
# RUN_ONCE=1

# Optional: MQTT settings (if not using core-mosquitto)
# MQTT_HOST=localhost
# MQTT_PORT=1883
# MQTT_USER=
# MQTT_PASSWORD=
