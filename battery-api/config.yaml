name: Battery API
description: Control SAJ Electric battery inverters via Home Assistant entities - schedule charging and discharging
version: "0.2.20"
slug: battery_api
init: false
homeassistant_api: true
arch:
  - aarch64
  - amd64
  - armhf
  - armv7
  - i386

# Options schema for UI-managed settings
options:
  provider: "api"
  # SAJ Electric Cloud API credentials
  saj_username: ""
  saj_password: ""
  device_serial_number: ""
  plant_uid: ""
  
  # Polling and behavior settings
  poll_interval_seconds: 60
  log_level: "info"
  simulation_mode: false
  modbus_inverter_power_w: 8000
  modbus_entities: {}
  
  # Schedule settings
  schedule_days: "today"  # "today" = only current day, "all" = all days Mon-Sun
  
  # MQTT settings for Home Assistant Mosquitto broker
  # Get credentials from Mosquitto add-on configuration
  mqtt_host: "core-mosquitto"
  mqtt_port: 1883
  mqtt_user: ""
  mqtt_password: ""

# Schema defines the data types for validation
schema:
  provider: list(api|modbus_ha)?
  saj_username: str?
  saj_password: password?
  device_serial_number: str?
  plant_uid: str?
  poll_interval_seconds: int(10,300)?
  log_level: list(debug|info|warning|error)?
  simulation_mode: bool?
  modbus_inverter_power_w: int(1000,30000)?
  modbus_entities: dict?
  schedule_days: list(today|all)?
  # Advanced: MQTT settings (optional - auto-detects core-mosquitto by default)
  mqtt_host: str?
  mqtt_port: int?
  mqtt_user: str?
  mqtt_password: password?
