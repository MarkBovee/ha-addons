# Energy Prices Add-on - Local Testing Configuration
# Copy this file to .env and fill in your values

# Nord Pool Configuration
DELIVERY_AREA=NL
CURRENCY=EUR
TIMEZONE=CET

# Price Calculation Templates (Jinja2)
# Import price template (including VAT, grid fees, energy tax)
IMPORT_PRICE_TEMPLATE={% set marktprijs = marktprijs %}{% set opslag_inc = 2.48 %}{% set energiebelasting_inc = 12.28 %}{% set btw = 1.21 %}{{ (marktprijs * btw + opslag_inc + energiebelasting_inc) | round(4) }}

# Export price template (feed-in tariff)
EXPORT_PRICE_TEMPLATE={{ marktprijs | round(4) }}

# Fetch interval in minutes (how often to update prices)
FETCH_INTERVAL_MINUTES=60

# Home Assistant Configuration
# Get your long-lived access token from HA:
# Profile > Security > Long-Lived Access Tokens > Create Token
SUPERVISOR_TOKEN=your_ha_supervisor_token_here
