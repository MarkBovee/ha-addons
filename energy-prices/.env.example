# Energy Prices Add-on - Local Testing Configuration
# Copy this file to .env and fill in your values

# Nord Pool Configuration
DELIVERY_AREA=NL
CURRENCY=EUR
TIMEZONE=CET

# Import Price Components
# Formula: (market_price * VAT_MULTIPLIER) + MARKUP + ENERGY_TAX
# Dutch defaults: 21% VAT (1.21), 2.48 ct/kWh markup, 12.28 ct/kWh energy tax
IMPORT_VAT_MULTIPLIER=1.21
IMPORT_MARKUP=2.48
IMPORT_ENERGY_TAX=12.28

# Export Price Components
# Formula: (market_price * VAT_MULTIPLIER) + MARKUP + ENERGY_TAX
# Default: market price only (no VAT, markup, or tax)
EXPORT_VAT_MULTIPLIER=1.0
EXPORT_MARKUP=0.0
EXPORT_ENERGY_TAX=0.0

# Fetch interval in minutes (how often to update prices)
FETCH_INTERVAL_MINUTES=60

# Home Assistant Configuration
# Get your long-lived access token from HA:
# Profile > Security > Long-Lived Access Tokens > Create Token
# Note: For local testing, use HA_API_TOKEN. SUPERVISOR_TOKEN is only used
# when running as an HA add-on with Supervisor access.
HA_API_TOKEN=your_ha_long_lived_token_here
